//a
let speed = (kmph > 60) ? "Fast" : "Normal";
console.log(speed);

//b
console.log(age >= 18 ? "Adult" : "Minor");


//c
const result =
  num > 0 ? "Positive" :
  num === 0 ? "Zero" :
  "Negative";
console.log(result);